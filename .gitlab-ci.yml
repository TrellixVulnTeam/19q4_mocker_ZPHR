image: docker:git
services:
- docker:dind

build:
  tags:
    - docker-atp
  only:
    - master
  script:
    - docker build -t test .
    - docker run --privileged test  # btrfs mounting requires --privileged

